<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TwentyFortyEight.ViewModels;assembly=TwentyFortyEight.ViewModels"
             xmlns:components="clr-namespace:TwentyFortyEight.Maui.Components"
             xmlns:behaviors="clr-namespace:TwentyFortyEight.Maui.Behaviors"
             xmlns:core="clr-namespace:TwentyFortyEight.Core;assembly=TwentyFortyEight.Core"
             xmlns:strings="clr-namespace:TwentyFortyEight.Maui.Resources.Strings"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
             x:Class="TwentyFortyEight.Maui.MainPage"
             x:DataType="local:GameViewModel"
             BackgroundColor="{AppThemeBinding Light=#faf8ef, Dark=#1a1a1a}">

    <ContentPage.ToolbarItems>
        <ToolbarItem
            Text="{x:Static strings:AppStrings.Settings}"
            Command="{Binding OpenSettingsCommand}"
            Order="Secondary"
            Priority="0" />
        <ToolbarItem
            Text="{x:Static strings:AppStrings.HowToPlay}"
            Command="{Binding ShowHowToPlayCommand}"
            Order="Secondary"
            Priority="1" />
    </ContentPage.ToolbarItems>

    <Grid x:Name="RootLayout" Padding="20" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header with Title and Score -->
        <Grid x:Name="HeaderGrid" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0"
                   Text="{x:Static strings:AppStrings.GameTitle}"
                   FontSize="48"
                   FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light=#776e65, Dark=#f9f6f2}"
                   VerticalOptions="Center"
                   SemanticProperties.HeadingLevel="Level1" />

            <VerticalStackLayout x:Name="ScoreContainer" Grid.Column="1" Grid.ColumnSpan="2" Spacing="10" Orientation="Horizontal">
                <Border x:Name="ScoreBorder"
                        BackgroundColor="{AppThemeBinding Light=#bbada0, Dark=#3c3a32}"
                        Stroke="Transparent"
                        StrokeThickness="0"
                        Padding="20,10"
                        Margin="10,0"
                        HeightRequest="60"
                        StrokeShape="RoundRectangle 5">
                    <VerticalStackLayout Spacing="0">
                        <Label Text="{x:Static strings:AppStrings.Score}"
                               FontSize="12"
                               FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light=#eee4da, Dark=#cdc1b4}"
                               HorizontalOptions="Center" />
                        <Label Text="{Binding Score}"
                               FontSize="20"
                               FontAttributes="Bold"
                               TextColor="White"
                               HorizontalOptions="Center"
                               SemanticProperties.Description="{x:Static strings:AppStrings.CurrentScoreDescription}" />
                    </VerticalStackLayout>
                </Border>

                <Border x:Name="BestScoreBorder"
                        BackgroundColor="{AppThemeBinding Light=#bbada0, Dark=#3c3a32}"
                        Stroke="Transparent"
                        StrokeThickness="0"
                        Padding="20,10"
                        HeightRequest="60"
                        StrokeShape="RoundRectangle 5">
                    <VerticalStackLayout Spacing="0">
                        <Label Text="{x:Static strings:AppStrings.Best}"
                               FontSize="12"
                               FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light=#eee4da, Dark=#cdc1b4}"
                               HorizontalOptions="Center" />
                        <Label Text="{Binding BestScore}"
                               FontSize="20"
                               FontAttributes="Bold"
                               TextColor="White"
                               HorizontalOptions="Center"
                               SemanticProperties.Description="{x:Static strings:AppStrings.BestScoreDescription}" />
                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
        </Grid>

        <!-- Status Text -->
        <Label Grid.Row="1"
               Text="{Binding StatusText}"
               FontSize="24"
               FontAttributes="Bold"
               TextColor="#f67c5f"
               HorizontalOptions="Center"
               IsVisible="{Binding StatusText, Converter={StaticResource StringNotEmptyConverter}}" />

        <!-- Game Board -->
        <Border x:Name="BoardContainer"
                Grid.Row="2"
                BackgroundColor="{AppThemeBinding Light=#bbada0, Dark=#3c3a32}"
                Stroke="Transparent"
                StrokeThickness="0"
                Padding="10"
                VerticalOptions="Center"
                HorizontalOptions="Center"
                MaximumWidthRequest="500"
                MaximumHeightRequest="500"
                StrokeShape="RoundRectangle 10">

            <Border.Behaviors>
                <behaviors:RippleBehavior />
            </Border.Behaviors>

            <Grid>
                <!-- Grid for tiles - rows and columns are generated dynamically in code-behind -->
                <Grid x:Name="GameBoard"
                      ColumnSpacing="10"
                      RowSpacing="10"
                      WidthRequest="400"
                      HeightRequest="400"
                      SemanticProperties.Description="{x:Static strings:AppStrings.GameBoardDescription}">
                    <!-- Tiles will be added programmatically in code-behind -->
                </Grid>
            </Grid>
        </Border>

        <!-- Controls -->
        <HorizontalStackLayout x:Name="ControlsContainer" Grid.Row="3" Spacing="15" HorizontalOptions="Center" Margin="0,10,0,0">

            <Border BackgroundColor="{AppThemeBinding Light=#8f7a66, Dark=#5a4a3a}"
                    StrokeThickness="0"
                    HeightRequest="50"
                    Padding="15,0"
                    StrokeShape="RoundRectangle 5"
                    SemanticProperties.Description="{x:Static strings:AppStrings.NewGameDescription}">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NewGameCommand}" />
                </Border.GestureRecognizers>
                <HorizontalStackLayout Spacing="8" VerticalOptions="Center">
                    <Label Text="↻"
                           FontSize="18"
                           TextColor="White"
                           VerticalOptions="Center" />
                    <Label Text="{x:Static strings:AppStrings.NewGame}"
                           FontAttributes="Bold"
                           TextColor="White"
                           VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Border>

            <Border BackgroundColor="{AppThemeBinding Light=#8f7a66, Dark=#5a4a3a}"
                    StrokeThickness="0"
                    HeightRequest="50"
                    Padding="15,0"
                    StrokeShape="RoundRectangle 5"
                    SemanticProperties.Description="{x:Static strings:AppStrings.UndoDescription}">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding UndoCommand}" />
                </Border.GestureRecognizers>
                <HorizontalStackLayout Spacing="8" VerticalOptions="Center">
                    <Label Text="↩"
                           FontSize="18"
                           TextColor="White"
                           VerticalOptions="Center" />
                    <Label Text="{x:Static strings:AppStrings.Undo}"
                           FontAttributes="Bold"
                           TextColor="White"
                           VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Border>

            <Border BackgroundColor="{AppThemeBinding Light=#8f7a66, Dark=#5a4a3a}"
                    StrokeThickness="0"
                    HeightRequest="50"
                    Padding="15,0"
                    StrokeShape="RoundRectangle 5"
                    SemanticProperties.Description="{x:Static strings:AppStrings.StatisticsDescription}">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding OpenStatsCommand}" />
                </Border.GestureRecognizers>
                <HorizontalStackLayout Spacing="8" VerticalOptions="Center">
                    <Label Text="📊"
                           FontSize="18"
                           TextColor="White"
                           VerticalOptions="Center" />
                    <Label Text="{x:Static strings:AppStrings.Statistics}"
                           FontAttributes="Bold"
                           TextColor="White"
                           VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Border>
        </HorizontalStackLayout>

        <!-- Game Over Modal -->
        <components:GameOverModal Grid.RowSpan="4"
                                  IsVisible="{Binding IsGameOver}"
                                  Score="{Binding Score}"
                                  BestScore="{Binding BestScore}"
                                  NewGameCommand="{Binding NewGameCommand}"
                                  ZIndex="100" />

        <!-- How To Play Modal -->
        <components:HowToPlayModal Grid.RowSpan="4"
                                   IsVisible="{Binding IsHowToPlayVisible}"
                                   CloseCommand="{Binding CloseHowToPlayCommand}"
                                   ZIndex="100" />
    </Grid>
</ContentPage>
